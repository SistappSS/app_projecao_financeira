<div class="form-check col-{{$col}} offset-{{$set}}">
    <input class="form-check-input" type="{{$type}}" name="{{$name}}" id="{{$id}}" value="{{$id}}" {{$check == null ? "" : "checked"}}>
    <label class="form-check-label" for="{{$id}}">{{$label}}</label>
</div>

